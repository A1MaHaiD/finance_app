<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/reports">Персональний менеджер фінансів</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if current_user.present? %>
            <a class="nav-link" href="/categories/" id="categoriesDropdown" role="button">
              Категорії
            </a>
          <ul class="dropdown-menu" aria-labelledby="categoriesDropdown">
            <!-- Dropdown items here -->
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="activitiesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Транзакції
          </a>
          <% end %>
          <ul class="dropdown-menu" aria-labelledby="activitiesDropdown">
            <% if current_user.present? && current_user.categories.any? %>
              <% current_user.categories.all.each do |category| %>
                <li>
                  <%= link_to category.name, category_operations_path(category), class: "dropdown-item" %>
                </li>
              <% end %>
              <li>
                <%= link_to "Усі транзакції", all_operations_path, class: "dropdown-item" %>
              </li>
            <% else %>
              <li>
                <span class="dropdown-item disabled">Усі транзакції</span>
              </li>
            <% end %>
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="helpDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Допомога
          </a>
          <ul class="dropdown-menu" style="padding-inline: 20px" aria-labelledby="helpDropdown">
              Дві три неділі і буде додано
          </ul>
          <li class="nav-item dropdown">
            <% if current_user.present? %>
              <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <%= current_user.email %>
              </a>
              <ul class="dropdown-menu" aria-labelledby="userDropdown">
                <li><%= link_to "Профіль", edit_user_registration_path, class: "dropdown-item" %></li>
                <li><%= link_to "Вийти", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
              </ul>
            <% else %>
              <a class="nav-link" href="<%= new_user_session_path %>">Увійти</a>
            <% end %>
          </li>
      </ul>
    </div>
  </div>
</nav>
